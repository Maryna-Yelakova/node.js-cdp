import express from 'express';import { groupHandler } from './handlers/group-handler';const groupRouter = express.Router();groupRouter.get("/groups", groupHandler.getGroups);groupRouter.get("/groups/:id", groupHandler.getGroupById);groupRouter.post("/groups", groupHandler.createGroup);groupRouter.put("/groups/:id", groupHandler.editGroup);//http://localhost:3000/groups/4groupRouter.delete("/groups/:id", groupHandler.deleteGroup);export default groupRouter;